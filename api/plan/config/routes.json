{
  "routes": [
    {
      "method": "GET",
      "path": "/plan/user",
      "handler": "Plan.findUserPlan",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/plan",
      "handler": "Plan.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/plan/:_id",
      "handler": "Plan.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/plan",
      "handler": "Plan.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/plan/:_id",
      "handler": "Plan.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/plan/:_id",
      "handler": "Plan.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
