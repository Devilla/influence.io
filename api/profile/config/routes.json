{
  "routes": [
    {
      "method": "GET",
      "path": "/profile",
      "handler": "Profile.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/profile/:_id",
      "handler": "Profile.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/profile",
      "handler": "Profile.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/profile/:_id",
      "handler": "Profile.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/profile/:_id",
      "handler": "Profile.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
