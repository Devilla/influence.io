  {
  "routes": [
    {
      "method": "GET",
      "path": "/es/health",
      "handler": "Elasticsearch.health",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/es/search",
      "handler": "Elasticsearch.query",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/es/search/:_id",
      "handler": "Elasticsearch.notification",
      "config": {
        "policies": []
      }
    }
  ]
}
