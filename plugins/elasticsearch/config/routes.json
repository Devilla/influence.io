{
  "routes": [
    {
      "method": "GET",
      "path": "/es",
      "handler": "Elasticsearch.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/es/health",
      "handler": "Elasticsearch.health",
      "config": {
        "policies": []
      }
    }
  ]
}
